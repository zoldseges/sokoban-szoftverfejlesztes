# Warning
The application creates a `sokoban.json` file in the users home directory.
Use `-Djavafx.args=sample-assets/sample-lib.json` to run with the provided samples instead.
`mvn javafx:run -Djavafx.args=sample-assets/sample-lib.json`